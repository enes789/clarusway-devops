apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    name: web
    app: todoapp
spec:
  selector:
    name: web-server
  type: NodePort
  ports:
   - name: http
     port: 30001
     targetPort: 80
     protocol: TCP
